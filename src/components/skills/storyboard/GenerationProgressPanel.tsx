/**
 * Phase 3: GenerationProgressPanel (stub)
 *
 * 実装は #44 で行う。カット生成の進捗を可視化する UI。
 */
export function GenerationProgressPanel() {
  return (
    <div className="flex h-full items-center justify-center text-zinc-500">
      <div className="text-sm">Phase 3: Generation Progress (実装中)</div>
    </div>
  );
}
